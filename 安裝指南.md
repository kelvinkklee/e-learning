# 香港小學樂學 · 安裝指南

個 App 已經係一個 **PWA（漸進式網頁應用）**，可以直接安裝落手機、平板或電腦，安裝後會好似原生 App 咁有自己嘅圖示、全螢幕開啟，仲可以離線使用。

---

## 🤖 AI 智能功能設定（可選）

個 App 內置 AI 智能出題、批改、錯題分析功能，需要先喺「AI 設定」填寫 LLM 資料：

1. 撳 App 頂部右上角嘅 **🤖 按鈕**
2. **供應商**：預設揀「華為雲 MaaS（經代理，推薦）」
3. **API Endpoint**：預設已填好代理地址（無需改）
   - `https://hk-learning-llm-proxy.kelvinkklee-hk-learning.workers.dev`
4. **模型名稱**：預設 `glm-5.2`（無需改）
5. **API Key**：填你嘅華為雲 MaaS API Key
6. 撳 **「測試連接」** → 見到「連接成功」即 OK（會自動儲存）
7. 之後喺任何單元頁撳 **「🤖 AI 出題」** 即可

> ⚠️ API Key 只儲存喺你本機（localStorage），唔會上傳。
> 如需直連華為（唔經代理），供應商可改揀「華為雲 MaaS（直連）」，但瀏覽器可能因 CORS 限制而失敗，建議用代理。

---

## 📱 Android（手機 / 平板）

用 **Chrome** 或 **Edge** 瀏覽器打開個 App 網址，然後：

1. 等頁面完全載入
2. 畫面頂部會出現 **「📲 安裝 App」** 按鈕，撳落去即可
3. 或者撳右上角 **⋮ 選單** → 揀 **「安裝應用程式」／「加到主畫面」**
4. 撳「安裝」確認，桌面就會多咗個「小學樂學」圖示

> 提示：安裝後喺桌面撳圖示，會用全螢幕（冇瀏覽器地址欄）開啟。

---

## 🍎 iPhone / iPad（iOS）

用 **Safari** 瀏覽器打開個 App 網址：

1. 撳底部嘅 **「分享」** 按鈕（方框加向上箭嘴）
2. 喺選單向下捲，揀 **「加到主畫面」**
3. 撳右上角 **「新增」**
4. 桌面就會出現「小學樂學」圖示，撳落去好似原生 App 咁開啟

> iOS 唔支援自動彈出安裝提示，一定要用「加到主畫面」呢個步驟。

---

## 🖥️ 電腦（Windows / macOS）

用 **Chrome** 或 **Edge** 瀏覽器：

1. 打開 App 網址
2. 撳地址欄右邊嘅 **安裝圖示**（⊕ 或 🖥 帶向下箭嘴）
3. 撳「安裝」
4. 會喺桌面／開始列建立捷徑，獨立視窗開啟

---

## 🌐 部署上網（令學生/家長真正用到）

而家個 App 喺本地沙箱運行緊，要真正畀人用，需要部署到一個網址（HTTPS）。兩個方向：

| 方案 | 適合 | 成本 | 難度 |
|---|---|---|---|
| **靜態託管**（Vercel / Netlify / GitHub Pages） | 快速上線、免費 | 免費 | 低 |
| **正式域名 + CDN**（自建） | 商用、有品牌域名 | 需伺服器/域名 | 中 |
| **打包成 APK**（Android 安裝檔） | 上架或內部派發 | 需 Android Studio | 中高 |

> PWA 安裝必須經 **HTTPS** 先會生效，所以一定要先部署到支援 HTTPS 嘅地方。

---

## 📦 打包成原生 APK（可選）

如果你想直接派一個 `.apk` 檔案畀學生安裝（唔使開瀏覽器），可以用 **Bubblewrap（TWA）** 將呢個 PWA 打包成 Android App：

```
npx @bubblewrap/cli init --manifest https://你的網址/manifest.json
npx @bubblewrap/cli build
```

> 前提同樣係要先有一個 HTTPS 網址。

---

## ✅ 已具備嘅安裝能力

- ✅ `manifest.json`（完整 PWA 配置：name、icons、start_url、scope、shortcuts）
- ✅ 唔同尺寸圖示（192 / 512 / maskable，Android 安裝用）
- ✅ iOS 專屬標籤（apple-touch-icon、apple-mobile-web-app-capable）
- ✅ Service Worker（離線快取，安裝後冇網絡都用到）
- ✅ 安裝提示按鈕（`beforeinstallprompt` 觸發）
